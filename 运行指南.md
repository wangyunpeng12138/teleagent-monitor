---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '6ba4c4fa-b9e9-4e87-be1f-c8ccb151b273'
  PropagateID: '6ba4c4fa-b9e9-4e87-be1f-c8ccb151b273'
  ReservedCode1: 'd42f97ab-fafa-4ae6-a063-bd55b96e77cb'
  ReservedCode2: 'd42f97ab-fafa-4ae6-a063-bd55b96e77cb'
---

# TeleAgent Monitor 运行指南

## 一、环境要求

运行前请确认以下环境已安装（首次开发时已配置好）：

| 环境 | 版本要求 | 安装路径 | 验证命令 |
|------|---------|---------|---------|
| Rust | 1.98+ | `D:\app\rust` | `rustc --version` |
| Node.js | 18+ | 系统安装 | `node --version` |
| VS 2022 | Community + C++ 工具链 | `C:\Program Files\Microsoft Visual Studio\2022\Community` | — |

> Rust 的环境变量 `RUSTUP_HOME` 和 `CARGO_HOME` 已设为用户级永久变量，但**新开的终端不一定自动刷新**。下面的运行脚本会手动设置。

---

## 二、快速运行（直接双击）

### 方式一：双击运行 release 版 exe（推荐）

已完成 release 编译，可直接运行独立 exe，**不需要任何环境**：

```
E:\agentWork\test\teleagent-monitor\src-tauri\target\release\teleagent-monitor.exe
```

双击即可启动，窗口大小 5.31 MB，不依赖 Node.js / Rust / MSVC 环境。

### 方式二：一键启动脚本（开发模式）

项目目录下有一键运行脚本：

```
E:\agentWork\test\teleagent-monitor\run.bat
```

**双击即可启动**。脚本会自动完成：
1. 初始化 MSVC C++ 编译环境
2. 设置 Rust 环境变量
3. 先构建前端（`npm run build`）
4. 再启动 Tauri 应用（`npx tauri dev`，支持热更新）

---

## 三、手动运行（命令行）

### 3.1 直接运行已编译的 release exe（推荐）

release 版本已编译完成，可直接独立运行，**无需任何开发环境**：

```bat
E:\agentWork\test\teleagent-monitor\src-tauri\target\release\teleagent-monitor.exe
```

> 这是推荐的使用方式，双击即可，不需要 Node.js / Rust / MSVC 环境。

### 3.2 开发模式（热更新，需要完整环境）

开发模式下修改前端代码会自动刷新，修改 Rust 代码会自动重编译：

```bat
:: 1. 打开 cmd 终端

:: 2. 初始化 MSVC 环境
call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat"

:: 3. 设置 Rust 环境变量
set RUSTUP_HOME=D:\app\rust\rustup
set CARGO_HOME=D:\app\rust\cargo
set PATH=D:\app\rust\cargo\bin;%PATH%

:: 4. 进入项目目录
cd /d E:\agentWork\test\teleagent-monitor

:: 5. 启动开发模式
npx tauri dev
```

### 3.3 生产构建（打包 exe 安装程序）

```bat
:: 同样先初始化环境（同上步骤 2-4）

:: 构建生产版本（会生成优化后的 exe + 安装包）
npx tauri build
```

构建产物位于：
```
src-tauri\target\release\teleagent-monitor.exe          ← 单文件可执行程序
src-tauri\target\release\bundle\nsis\*.exe              ← NSIS 安装包
src-tauri\target\release\bundle\msi\*.msi               ← MSI 安装包
```

---

## 四、运行后说明

### 4.1 窗口行为

| 行为 | 操作方式 |
|------|---------|
| 拖动窗口 | 按住顶部标题栏拖拽 |
| 刷新数据 | 点击标题栏 🔄 按钮 |
| 展开/折叠任务 | 点击会话卡片切换展开/折叠（默认折叠，显示状态+进度摘要） |
| 隐藏到托盘 | 点击标题栏 ✕ 按钮，窗口隐藏但进程不退出 |
| 从托盘恢复 | 左键点击系统托盘图标，切换窗口显示/隐藏 |
| 显示面板 | 右键托盘图标 → 选择「显示面板」 |
| 隐藏到托盘 | 右键托盘图标 → 选择「隐藏到托盘」 |
| 退出程序 | 右键托盘图标 → 选择「退出程序」 |

### 4.2 数据刷新机制

| 数据 | 刷新方式 | 频率 |
|------|---------|------|
| 交互会话 + Todo | 定时轮询 | 每 3 秒 |
| 定时任务 | 定时轮询 | 每 10 秒 |
| 会话增减 | 文件监听 | 事件驱动（session-status.json 变更时立即触发） |

### 4.3 数据来源

悬浮窗读取以下 TeleAgent 本地文件，**不修改任何数据**：

| 文件 | 路径 | 读取方式 |
|------|------|---------|
| 会话状态 | `%USERPROFILE%\.local\share\TeleAgent\session-status.json` | JSON 解析 |
| 会话+任务 | `%USERPROFILE%\.local\share\TeleAgent\teleagent.db` | SQLite 只读 (immutable) |
| 定时任务 | `%USERPROFILE%\.local\share\TeleAgent\scheduler\scheduler.db` | SQLite 只读 (immutable) |

### 4.4 日志查看

Rust 后端日志输出到 **stderr**，开发模式下直接在终端窗口可见。

日志级别和内容示例：
```
[2026-09-04T21:30:00.123] [INFO] [main] Tauri 应用初始化完成
[2026-09-04T21:30:00.456] [INFO] [watcher] 文件监听已启动
[2026-09-04T21:30:03.789] [INFO] [cmd] fetch_running_sessions 被调用
[2026-09-04T21:30:03.790] [INFO] [db] session-status.json 解析成功，2 个会话
[2026-09-04T21:30:03.791] [INFO] [db] 数据库已打开(immutable): .../teleagent.db
[2026-09-04T21:30:03.792] [INFO] [db] 查询到 2 条 session 记录
[2026-09-04T21:30:03.793] [INFO] [cmd] 返回 2 个会话
```

如需将日志写入文件，在运行前设置环境变量：
```bat
set RUST_LOG=info
:: 或更详细的日志
set RUST_LOG=debug
```

---

## 五、常见问题

### Q: 启动后窗口空白/不显示数据

**原因**：TeleAgent 未运行，或数据目录不存在。

**检查**：
1. 确认 `C:\Users\你的用户名\.local\share\TeleAgent\` 目录存在
2. 确认 `teleagent.db` 文件存在
3. 确认 TeleAgent 桌面客户端已启动并运行过至少一次

### Q: 提示 "数据库文件不存在"

TeleAgent 可能尚未创建数据库。启动一次 TeleAgent 桌面客户端后再试。

### Q: `npx tauri dev` 报错 "rustup could not choose a version"

Rust 环境变量未设置。在运行 `npx tauri dev` 之前，先执行：
```bat
set RUSTUP_HOME=D:\app\rust\rustup
set CARGO_HOME=D:\app\rust\cargo
set PATH=D:\app\rust\cargo\bin;%PATH%
```

### Q: 编译报错 "link.exe not found"

MSVC 环境未初始化。先运行：
```bat
call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat"
```

### Q: 窗口不置顶 / 不透明

确保使用的是 Windows 10/11，且 WebView2 Runtime 已安装。大多数 Windows 11 已预装 WebView2。

---

## 六、项目文件一览

```
teleagent-monitor/
├── run.bat                        ← 一键运行脚本
├── package.json                   ← 前端依赖配置
├── vite.config.ts                 ← Vite 构建配置
├── index.html                     ← HTML 入口
├── src/                           ← Vue3 前端源码
│   ├── App.vue                    ← 主组件（标题栏+布局+标签过滤）
│   ├── main.ts                    ← Vue 入口
│   ├── components/
│   │   ├── SessionCard.vue        ← 会话卡片
│   │   ├── TodoItem.vue           ← 单个任务条目
│   │   └── SchedulerPanel.vue     ← 定时任务行
│   ├── composables/
│   │   ├── useMonitor.ts          ← 数据采集与刷新逻辑
│   │   ├── useWindowDrag.ts       ← 窗口拖拽
│   │   ├── logger.ts              ← 前端日志
│   │   └── types.ts               ← TypeScript 类型定义
│   └── assets/
│       └── style.css              ← 全局样式
└── src-tauri/                     ← Rust 后端源码
    ├── Cargo.toml                 ← Rust 依赖配置
    ├── tauri.conf.json            ← Tauri 窗口配置
    ├── build.rs                   ← Tauri 构建脚本
    ├── capabilities/
    │   └── default.json           ← 权限配置
    ├── icons/                     ← 应用图标
    └── src/
        ├── main.rs                ← 程序入口
        ├── db.rs                  ← SQLite 数据库读取
        ├── watcher.rs             ← 文件监听
        └── commands.rs            ← IPC 命令定义
```

> AI生成